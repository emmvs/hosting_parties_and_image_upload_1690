<div class="container mb-3">
  <p style="color: green"><%= notice %></p>

  <%= render @student %>

  <div>
    <%= link_to "Edit this student", edit_student_path(@student), class: 'btn' %> |
    <%= link_to "Back to students", root_path, class: 'btn' %>

    <%= button_to "🗑️", @student, method: :delete, class: 'btn' %>
  </div>
</div>
